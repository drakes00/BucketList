services:
  pocketbase:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8090:8090"
    volumes:
      - pb_data:/pb/pb_data
    restart: unless-stopped

volumes:
  pb_data:
